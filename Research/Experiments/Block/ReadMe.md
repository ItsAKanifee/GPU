# Block Experiments

## Purpose

The idea of this folder is to make a device that can run multiple lanes in parallel that all share the same instruction but perform operations individually

## Goals

- [ ] Create and test a design that runs multiple (8) lanes at once that all share the same instruction
- [ ] Integrate a scheduler that can turn certain lanes on and off (will have to change lane schematic)
- [ ] Integrate PC and branch traversal within the block